Sub 二つ目に選択したセルから値をもってくる移動版()
    If Selection.Count = Selection.Areas.Count And Selection.Areas.Count = 2 Then
        Selection.Areas(1).Value = Selection.Areas(2).Value
        Selection.Areas(2).ClearContents
        
    End If
    
End Sub